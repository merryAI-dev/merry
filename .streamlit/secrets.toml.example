# Streamlit OAuth Configuration
# 이 파일을 secrets.toml로 복사하고 실제 값으로 변경하세요

[auth]
# 로컬 테스트: http://localhost:8501/oauth2callback
# Streamlit Cloud: https://your-app-name.streamlit.app/oauth2callback
redirect_uri = "https://your-app-name.streamlit.app/oauth2callback"

# 32자 이상의 랜덤 문자열 (보안용)
# 생성 방법: python -c "import secrets; print(secrets.token_hex(32))"
cookie_secret = "your-random-32-character-secret-here"

[auth.google]
# Google Cloud Console에서 발급
# https://console.cloud.google.com/apis/credentials
client_id = "your-google-client-id.apps.googleusercontent.com"
client_secret = "your-google-client-secret"

# Google OIDC 설정 (수정 불필요)
server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"
